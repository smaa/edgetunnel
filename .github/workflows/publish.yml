name: Publish to Cloudflare
on:
  #push:
  #schedule:
  #  - cron: "0 18 * * *" # every day, UTC 18:00 is 2:00AM at China
  workflow_dispatch:
  
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    name: Publish to Cloudflare Pages
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # Run a build step here if your project requires
      - name: Install dependencies
        run: npm install
      - name: Run build for uglifyjs
        run: npm run build
        
      - name: Publish to Cloudflare B
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ vars.CLOUDFLARE_API_TOKEN_B }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID_B }}
          # Optional: Enable this if you want to have GitHub Deployments triggered
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          # Optional: Change the working directory
          command: pages deploy ./dist --project-name=edge
        id: publish
      - name: Outputs of edge B
        run: |
          echo "ID: ${{ steps.publish.outputs.pages-deployment-id }}"
          echo "URL: ${{ steps.publish.outputs.deployment-url }}"
          echo "Environment: ${{ steps.publish.outputs.pages-environment }}"
          echo "Alias: ${{ steps.publish.outputs.pages-deployment-alias-url }}"

